@model IEnumerable<DrAbdelfatehSalma.Models.Chirurgie>
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Gestion des Chirurgies";
}

<div class="space-y-8">
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4 border-b border-gold/20 pb-6">
        <div>
            <h1 class="font-display-lg text-2xl font-bold text-white mb-1">Chirurgies (Catalogue Interventions)</h1>
            <p class="text-xs text-gray-400">Ajouter, modifier ou supprimer les fiches d'intervention chirurgicale.</p>
        </div>
        <a asp-controller="Admin" asp-action="CreateChirurgie" class="gold-gradient text-white font-label-caps text-xs py-3 px-5 hover:opacity-90 transition-all font-bold tracking-widest flex items-center gap-2 shadow">
            <span class="material-symbols-outlined text-sm">add</span>
            <span>AJOUTER UNE CHIRURGIE</span>
        </a>
    </div>

    <div class="bg-[#121419] border border-gold/20 overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full text-left text-xs text-gray-300">
                <thead class="bg-[#0c0d10] text-gold font-label-caps text-[10px] uppercase border-b border-gold/20">
                    <tr>
                        <th class="p-4">Image</th>
                        <th class="p-4">Titre</th>
                        <th class="p-4">Catégorie</th>
                        <th class="p-4">Durée</th>
                        <th class="p-4">Anesthésie</th>
                        <th class="p-4 text-right">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-800">
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr class="hover:bg-white/5 transition-colors">
                                <td class="p-4">
                                    <img src="@item.ImageUrl" alt="@item.Title" class="w-12 h-12 object-cover border border-gold/30 rounded" />
                                </td>
                                <td class="p-4 font-bold text-white">@item.Title</td>
                                <td class="p-4 text-gold">@item.Category</td>
                                <td class="p-4">@item.Duree</td>
                                <td class="p-4">@item.Anesthesie</td>
                                <td class="p-4 text-right space-x-2">
                                    <a asp-controller="Admin" asp-action="EditChirurgie" asp-route-id="@item.Id" class="px-3 py-1.5 bg-gold/20 text-gold hover:bg-gold hover:text-white rounded text-[11px] font-bold transition-all inline-block">
                                        Éditer
                                    </a>
                                    <form asp-controller="Admin" asp-action="DeleteChirurgie" asp-route-id="@item.Id" method="post" class="inline" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette chirurgie ?');">
                                        <button type="submit" class="px-3 py-1.5 bg-red-500/20 text-red-400 hover:bg-red-500 hover:text-white rounded text-[11px] font-bold transition-all">
                                            Supprimer
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="p-8 text-center text-gray-500 italic">Aucune chirurgie enregistrée.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
